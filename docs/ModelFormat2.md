Model Format 2.0: 
base off of archive format!
keep it common sense!
make use of multiple data outputs!

# Outputs
* Prefab Data: contains all node->entity heirarchy (nickname)
* Material (nickname/materials/MATERIAL_NAME)
* Geometry Data (render meshes, physics bakes, and splines) (nickname/geometry/MESH_NAME)
* Animation Channel Data (nickname/anim/ANIM_NAME)

Drop the following:
Navmesh should be generated by audition from collision meshes